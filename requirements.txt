scikit-learn>=1.4
torch>=2.2
numpy>=1.26
pandas>=2.2
scipy>=1.12
statsmodels>=0.14
optuna>=3.6
shap>=0.45
umap-learn>=0.5
matplotlib>=3.8
plotly>=5.20
seaborn>=0.13
tqdm>=4.66
joblib>=1.3
pyyaml>=6.0
pyarrow>=15
openpyxl>=3.1
# Live terminal dashboard (src/utils/console_ui.py).
rich>=13
# Process/system resource sampling shown in the dashboard during heavy phases
# (training, interpretability) -- see src/utils/resource_monitor.py. Kept out
# of src/utils/observability.py deliberately (see CONTEXT.md): that module's
# peak_python_memory_mb() stays stdlib-only (tracemalloc), this is a separate,
# explicitly-requested, dashboard-only concern.
psutil>=5.9
